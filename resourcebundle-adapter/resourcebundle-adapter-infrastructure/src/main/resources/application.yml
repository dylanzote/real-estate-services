spring:
  application:
    name: template-service
  profiles:
    include: common, database-migration
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/resource-bundle
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8083


